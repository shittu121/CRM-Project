import React from 'react';
import Profile from '@/components/company/profile' // Adjust the path if necessary

const Page: React.FC = () => {
  return (
    <div>
      <Profile />
    </div>
  );
};

export default Page;
